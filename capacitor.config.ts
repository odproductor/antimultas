import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.encontrable.antimultas',
  appName: 'Capitán Antimultas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
